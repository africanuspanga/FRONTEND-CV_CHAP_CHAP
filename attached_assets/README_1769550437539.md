# CV Chap Chap V2 - Landing Page

A modern, professional landing page for CV Chap Chap - Tanzania's AI-powered CV builder.

## 🎨 Design Features

- **Light blue brand colors** with gradient accents
- **Modern glassmorphism** effects
- **Smooth animations** (floating elements, scroll effects, hover states)
- **Mobile-first responsive** design
- **East African market** focused content
- **M-Pesa payment** integration messaging

## 📦 Quick Setup

### 1. Install Dependencies

```bash
npm install framer-motion lucide-react
# or
yarn add framer-motion lucide-react
# or
pnpm add framer-motion lucide-react
```

### 2. Add Google Fonts

Add this to your `app/layout.tsx` (Next.js App Router) or `pages/_document.js` (Pages Router):

```jsx
// app/layout.tsx
import { Plus_Jakarta_Sans, Space_Grotesk } from 'next/font/google'

const plusJakarta = Plus_Jakarta_Sans({
  subsets: ['latin'],
  variable: '--font-body',
})

const spaceGrotesk = Space_Grotesk({
  subsets: ['latin'],
  variable: '--font-display',
})

export default function RootLayout({ children }) {
  return (
    <html lang="en" className={`${plusJakarta.variable} ${spaceGrotesk.variable}`}>
      <body>{children}</body>
    </html>
  )
}
```

**OR** add via CDN in your HTML `<head>`:

```html
<link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=Space+Grotesk:wght@500;600;700&display=swap" rel="stylesheet" />
```

### 3. Update Tailwind Config

Copy the `tailwind.config.js` file or merge these settings into your existing config:

```js
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        'display': ['Space Grotesk', 'var(--font-display)', 'sans-serif'],
        'body': ['Plus Jakarta Sans', 'var(--font-body)', 'sans-serif'],
      },
      colors: {
        'cv-blue': {
          50: '#eff8ff',
          100: '#dbeefe',
          200: '#bfe3fe',
          300: '#93d3fd',
          400: '#60bbfa',
          500: '#3b9df6',
          600: '#2580eb',
          700: '#1d69d8',
          800: '#1e55af',
          900: '#1e498a',
        },
      },
    },
  },
}
```

### 4. Use the Component

```jsx
// app/page.tsx or pages/index.tsx
import CVChapChapLanding from '@/components/CVChapChapLanding'

export default function Home() {
  return <CVChapChapLanding />
}
```

## 📁 File Structure

```
your-project/
├── app/
│   ├── layout.tsx          # Add fonts here
│   └── page.tsx            # Import landing page
├── components/
│   └── CVChapChapLanding.jsx  # Main landing page component
├── tailwind.config.js      # Custom theme config
└── package.json
```

## 🔧 Customization

### Update Contact Info

Search for `xxxxx` in the component and replace with real contact info:

```jsx
// In the Footer section
<li>📧 info@cvchapchap.co.tz</li>
<li>📱 +255 123 456 789</li>
```

### Update Pricing

Find the pricing section and update the TZS amount:

```jsx
<span className="font-display text-6xl font-bold text-white">5,000</span>
```

### Add Real Testimonials

Replace the testimonials array with real customer feedback:

```jsx
const testimonials = [
  {
    name: 'Real Customer Name',
    role: 'Job Title',
    location: 'City',
    text: 'Their actual review...',
    avatar: 'RC', // Initials
  },
  // ...more testimonials
]
```

### Update Stats

Replace with real statistics:

```jsx
const stats = [
  { value: '50,000+', label: 'CVs Created' },
  { value: '21', label: 'Templates' },
  // ...
]
```

## 🎯 Sections Included

1. **Navigation** - Fixed header with scroll effect
2. **Hero** - Main headline with CV preview mockup
3. **Stats Bar** - Key metrics
4. **How It Works** - 4-step process
5. **Features** - 6 feature cards
6. **Templates** - Template showcase grid
7. **Testimonials** - Auto-rotating reviews
8. **Pricing** - Single pricing card
9. **FAQ** - Accordion questions
10. **CTA** - Final call-to-action
11. **Footer** - Links and contact info

## 📱 Responsive Breakpoints

- **Mobile**: < 768px
- **Tablet**: 768px - 1024px
- **Desktop**: > 1024px

## 🚀 Performance Tips

1. **Lazy load images** - Add `loading="lazy"` to images
2. **Optimize fonts** - Use `font-display: swap`
3. **Reduce animations** - Add `prefers-reduced-motion` media query
4. **Code split** - Import component dynamically if not above the fold

## 📄 License

Proprietary - CV Chap Chap © 2025

---

Built with ❤️ for the Tanzanian job market
